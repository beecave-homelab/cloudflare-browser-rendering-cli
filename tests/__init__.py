"""Integration test package for *cloudflare_browser_render*.

Contains pytest-based smoke tests that validate public interfaces of the
project (CLI entry-points, importability, renderer behaviour, etc.).
Only test helpers and fixtures should live here.
"""
